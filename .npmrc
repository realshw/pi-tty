# npmmirror (Taobao) mirror.
# The committed lockfile records matching `resolved` URLs; override for a
# single install with `npm install --registry=https://registry.npmjs.org/`.
registry=https://registry.npmmirror.com/
